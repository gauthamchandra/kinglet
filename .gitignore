# Dependencies
node_modules/
bun.lockb

# Build outputs
dist/
build/
out/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
.coverage/
*.lcov

# Test output
test-results/
.bun/

# IDE files
.vscode/
.idea/
*.swp
*.swo
*.iml

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Docker
.docker/

# Temporary files
tmp/
temp/

# Terraform — ignore lock files except the pinned harness lock in terraform/
.terraform.lock.hcl
!terraform/.terraform.lock.hcl

# Data directory (for local development)
data/

# Compatibility audit reports (generated)
.compatibility-reports/*
!.compatibility-reports/.gitkeep

# Discovery document cache (fetched from Google at generate time; not committed)
.discovery-cache/

# Agent scratch space (Conductor workspaces, session notes)
.context/

# Per-developer Claude Code overrides. The shared allowlist that contributors
# inherit lives in the checked-in .claude/settings.json; this file is where your
# own additions accumulate and is personal by convention.
.claude/settings.local.json

# Don't commit discovery documents that GCP gates behind authentication
# because it's likely against their wishes
memorystore.discovery-document.private.json
